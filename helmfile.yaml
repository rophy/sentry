helmDefaults:
  createNamespace: false
  timeout: 1200
repositories:
- name: sentry
  url: https://sentry-kubernetes.github.io/charts
releases:
- name: sentry
  chart: sentry/sentry
  values:
  - values.yaml
